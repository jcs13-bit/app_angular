import { FormatoMonedaCopPipe } from './formato-moneda-cop.pipe';

describe('FormatoMonedaCopPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoMonedaCopPipe();
    expect(pipe).toBeTruthy();
  });
});
